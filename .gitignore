node_modules/
.env
.env.local
.env.production
.env.*.local
.env.*
secrets/
*.pem
*.key
*.jks
*.p12
*.pfx
*.crt
data/*.json
data/*.sqlite
data/*.sqlite-journal
data/*.sqlite-shm
data/*.sqlite-wal
data/*.db
data/cache/
*.log
.DS_Store
.vercel/
.vercel
public/img/_generations/
.idea/
.vscode/
.claude/
.agent/
*.swp
*.swo
.cache/
dist/
!packages/sdk-ts/dist/
!packages/sdk-ts/dist/**
!packages/sdk-rn/dist/
!packages/sdk-rn/dist/**
.npm-cache*/
**/.npm-cache*/
build/
coverage/
.nyc_output/
.tmp/
.tmp.*
*.bak
.netrc
~/

# Python build artifacts (sdk/python)
__pycache__/
*.pyc
*.pyo

# v7.7 sim artifacts (raw runs); regenerable via scripts/sim-100.mjs
sim-100*.json
sim-100*.err
_audit/
tmp/

# QA screenshots from probe scripts (regenerable, not product code)
scripts/qa-*.png
scripts/video/qa-*.png
scripts/video/qa/

# Internal engineering specs — never commit
INTERNAL_BACKEND_SPEC.md

# wave 100 P2-4 — local ephemera that should never be tracked
tmp-*/
server-*.log
CUsersuser*/
backups/*-broken-*/

# W257 — additional local-ephemera scrub (must never reach git/public)
%TEMP%*
.kolm-bundle/
.tmp-w*/
.tmp-*/
undefined/

# Test ephemera (node --test tap output, per-run timing)
.test-output.tap
.test-output-serial.tap
.test-time.txt

# W470 — Rust build outputs (Cargo.lock IS committed; target/ is not)
sdk/rust/target/
packages/runtime-rs/target/
sdk/c/kolm-cli
sdk/c/kolm-cli.exe
sdk/c/*.o

# W470 — release-verify driver output
release-verify.out.json
release-verify.err.log
release-verify.full.json
release-verify.full.err
.audit-w*/
w[0-9]*-out.txt

# W471 — tesseract.js OCR language packs (downloaded on first run; huge, regenerable)
eng.traineddata
*.traineddata

# W471 — release-verify and ad-hoc test scratch
test-output.txt
test-sweep*.txt
*test-sweep*.txt
**/AppData/Local/Temp/**

# W547 — UI-surface audit output (timestamped runs, regenerable, multi-GB)
reports/
.ui-debug/
.shots/
.w605-body.html

# W890-7 — user/project config + capture store. The user TOML lives at
# ~/.kolm/config.toml (HOME, not repo) but if a contributor symlinks or
# copies the file into the repo tree these globs catch it. captures.db is
# the local SQLite capture store; *.db / *.sqlite under data/ are already
# ignored, this row makes the intent for the repo root explicit.
.kolm/
.kolm/config.toml
**/.kolm/config.toml
captures.db
**/captures.db

# Internal planning / audit docs — never publish
KOLM_*_PLAN.md
KOLM_*_MANUAL.md
FINAL_*.md
STRATEGY.md
DEMO.md
audit-shots/
.tmp-*
.tmp-*.txt
.tmp-*.py
release-verify-*.err
test-five*
UsersuserAppData*/
