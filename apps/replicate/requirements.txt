# Replicate Cog container deps. Cog reads python_packages from cog.yaml
# when building the image; this file is here so users can lint or
# install locally (uv pip install -r requirements.txt) before pushing.
# W890-13: every dep pinned with == so the local lint matches the prod image.
cog==0.10.2
torch==2.4.1
transformers==4.45.2
peft==0.13.2
datasets==3.0.1
accelerate==0.34.2
bitsandbytes==0.44.1
httpx==0.27.2
safetensors==0.4.5
sentencepiece==0.2.0
