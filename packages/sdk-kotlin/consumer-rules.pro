# Kolm Android SDK does not require consumer ProGuard keep rules by default.
# Runtime backends such as ExecuTorch, ONNX Runtime, or llama.cpp may add their
# own keep rules when installed by the host application.
